const marvel_heros = ['ironman', 'spiderman', 'doctor strange', 'wanda']
const dc_heros = ['batman', 'superman', 'rock', 'flash']
const names = new Array()

//Array Methods
// names.push('Beater')
// console.log(names);
// names.push('Ezio') // add items at last in the array
// console.log(names);
// names.pop() // remove last item from the array
// console.log(names);
// names.unshift('on_start') //add values at the start of the array
// console.log(names);
// names.shift() //remove values at the start of the array
// console.log(names);

// console.log(marvel_heros.includes('ironman'));
// console.log(marvel_heros.indexOf('spiderman'));


//slice and splice

// const a = marvel_heros.slice(1, 3)
// const b = marvel_heros.splice(1, 3) //manuplate original array and also count the last index